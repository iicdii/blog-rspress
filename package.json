{
  "name": "rspress-doc-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "rspress dev",
    "build": "rspress build",
    "preview": "rspress preview",
    "sync": "node --env-file .env ./sync-obsidian.js"
  },
  "dependencies": {
    "rspress": "^1.9.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "js-yaml": "^4.1.0"
  }
}
